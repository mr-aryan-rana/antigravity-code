import type { Node } from '../Node';
export declare const enum FsEventType {
    CREATE = 0,
    DELETE = 1,
    MODIFY = 2,
    MOVE = 3
}
export declare class FsEvent {
    readonly type: FsEventType;
    readonly steps: string[];
    readonly node: Node;
    readonly oldSteps: string[] | undefined;
    constructor(type: FsEventType, steps: string[], node: Node, oldSteps?: string[] | undefined);
}
